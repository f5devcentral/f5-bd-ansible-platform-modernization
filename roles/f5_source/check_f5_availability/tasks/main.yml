---

- name: Wait until device is offline (ping fails)
  ansible.builtin.command: "ping -c 1 -W 1 {{ ansible_host }}"
  register: ping_result
  ignore_errors: true
  until: ping_result.rc != 0
  retries: 30
  delay: 10
  delegate_to: localhost

---

- name: Wait until device is offline (ping fails)
  ansible.netcommon.net_ping:
    dest: "{{ ansible_host }}"
    count: 1
  register: f5_avail
  ignore_errors: true
  until: f5_avail.failed
  retries: 30
  delay: 30
  delegate_to: localhost
